# Mi primer repo :D

# Comandos para subir el proyecto

  1. git commit -m "mensaje del commit"
  2. git remote add origin https://.......  <- aquí va el enlace del nuevo repositorio creado en github
  3. git push -u origin main  <-- esto se ejecuta la primera vez, las veces posteriores se usa "git push" solamente